# arc-raiders-info-sheet

## Guide
- Run npx serve to start up local web server
- Runs on localhost port 3000
- Parses data.json to populate inventory
- export data.json button downloads a new json with inventory set as json items
    - replaces current data.json with exported
